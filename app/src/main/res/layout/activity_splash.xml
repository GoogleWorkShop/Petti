<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_splash"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@color/tw__composer_black"
    tools:context="com.firebase.petti.petti.SplashActivity">

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/splash_header"
        android:src="@drawable/petti_header"
        android:visibility="invisible"
        android:layout_alignParentTop="true"/>

    <ImageView

        android:visibility="invisible"
        android:id="@+id/splash_pic"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:src="@drawable/pet_pic_grey_burned"
        android:background="@color/tw__transparent"/>

    <TextView
        android:visibility="invisible"
        android:id="@+id/splash_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/splash_pic"
        android:layout_centerHorizontal="true"
        android:text="@string/slogen"
        android:textSize="30sp"
        android:typeface="sans"
        android:textColor="@color/tw__light_gray"/>

</RelativeLayout>
